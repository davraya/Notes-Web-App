<!DOCTYPE html>
<html>
<head>
  <title>Login Form</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      margin: 0;
      position: relative; /* Add this line to set the body as the relative positioning reference */
    }

    .container {
      max-width: 300px;
      padding: 20px;
      background-color: #fff;
      border: 1px solid #ccc;
      border-radius: 5px;
      position: relative; /* Add this line to set the container as the relative positioning reference */
    }

    .container h2 {
      text-align: center;
      margin-bottom: 20px;
    }

    .container label,
    .container input[type="text"],
    .container input[type="password"] {
      display: block;
      width: 100%;
      margin-bottom: 10px;
    }

    .container input[type="submit"],
    .container p {
      width: 100%;
      margin-bottom: 10px;
    }

    .container input[type="submit"] {
      background-color: #4CAF50;
      color: #fff;
      border: none;
      padding: 10px;
      cursor: pointer;
    }

    .container input[type="submit"]:hover {
      background-color: #45a049;
    }

    .container p {
      text-align: center;
    }

    .container p a {
      color: #007bff;
      text-decoration: none;
    }

    .flash-message-container {
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      text-align: center;
      margin: 20px 0;
    }

    .notice {
      background-color: #ffddc1;
      color: #f07373;
      border: 1px solid #f07373;
      padding: 10px;
      border-radius: 5px;
    }
  </style>
</head>
<body>
  <div class="flash-message-container">
    <%- messages() %>
  </div>
  <div class="container">
    <h2>Login Form</h2>
    <form action="/account/login" method="post">
      <label for="username">Username:</label>
      <input type="text" id="username" name="account_username" required>

      <label for="password">Password:</label>
      <input type="password" id="password" name="account_password" required>

      <input type="submit" value="Login">

      <p>Don't have an account? <a href="/account/register">Sign up</a></p>
    </form>
  </div>
</body>
</html>